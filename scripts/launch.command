#!/bin/bash

# Mac Auto-Launch Script for Hash Table Bid System
# Double-click this file in Finder to build and run the application

# Change to the directory where this script is located
cd "$(dirname "$0")"

# Run the build and launch script
./run.sh

# Keep terminal open after execution
echo ""
echo "Press any key to close this window..."
read -n 1
